# castles_of_crimson/tools

**Board ART rendering — NOT ML tooling.** `render_boards.py` draws the CoC board/tile images.

Do not confuse this with `rust-cores/coc-core/tools/`, which is the offline ML toolkit (net training,
harvests, gates) for the CoC Expert AI.
