"""Spender AI SERVING stack — the modules main.py imports at runtime to pick moves
(engine, actions, heuristic/valuation families, v_state, vsearch, mcts, infer_np, replay).
This is the deployed brain; the offline research toolkit lives in ../offline.
"""
